"use strict"; // turns strict mode on

// set numerator to 12
let numerator = 12;
// set denominator to 3
let denominator = 3;
// set result to numerator divided by denominator
let result = numerator / denominator;

// print "<numerator> divided by <denominator> is <result>"
console.log(`${numerator} divided by ${denominator} is ${result}`);
