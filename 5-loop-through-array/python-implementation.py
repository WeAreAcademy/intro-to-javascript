# set words to ["the", "enormous", "cat", "sat", "slouching", "on", "the", "horizontal", "mat"]
words = ["the", "enormous", "cat", "sat", "slouching", "on", "the", "horizontal", "mat"]

# for each word in words
for word in words:
    # if word has 3 letters or fewer
    if len(word) <= 3:
        # print word
        print(word)
    # else
    else:
        # print "*****"
        print("*****")
